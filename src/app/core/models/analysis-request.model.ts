/**
 * Modello per la richiesta di analisi (corrisponde al DTO Java AnalysisRequestDTO)
 * Inviato al backend per avviare una nuova analisi.
 */
export interface AnalysisRequest {
  profileURL: string;
}
